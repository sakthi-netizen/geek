int findMax(struct Height arr[], int n)
{
    int ans, max = INT_MIN;
    for(int i=0; i<n; i++){
        ans = arr[i].feet*12 + arr[i].inches;
        if(ans > max)max = ans; 
    }
    return max;
}